---

- name: Initial configuration for application server
  hosts: all
  become: yes
  become_user: root
  remote_user: root
  vars:
    - update_apt_cache: yes
  vars_files:
    - roles/base/defaults/main.yml

  roles:
    - security
