---

- name: Initial configuration for application server
  hosts: all
  become: true
  become_user: root
  remote_user: root
  vars:
    - update_apt_cache: true
  vars_files:
    - roles/base/defaults/main.yml

  roles:
    - security
