---
exclude_paths:
  - ./roles/geerlingguy.htpasswd/
  - ./roles/logdna.logdna/

skip_list:
  - ANSIBLE0006
  - ANSIBLE0012
  # TODO: Remove free-form from the repo.
  - no-free-form
  - name[missing]
  # TODO: Fix this.
  - name[casing]
  # TODO: Test removing this from each task and see if there is a workaround.
  - no-changed-when
  # TODO: Remove shell pipe from the repo
  - risky-shell-pipe
  # TODO: Remove these
  - command-instead-of-shell
  - package-latest
  - risky-file-permissions
  - command-instead-of-module
