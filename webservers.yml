---

- name: Provision application web server
  hosts: webservers
  become: yes
  become_user: root
  remote_user: "{{ server_user }}"
  vars:
    update_apt_cache: yes

  roles:
    - base
    - certbot
    - rabbitmq
    - web
    - celery
    - nginx
