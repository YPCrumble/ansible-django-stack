---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: instance-jammy
    groups:
      - vagrant
    image: ubuntu:jammy
    privileged: true
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../roles/
  # TODO: This is perhaps not required
  inventory:
    links:
      group_vars: ../../group_vars/
verifier:
  name: ansible
