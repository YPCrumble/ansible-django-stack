---

- name: Create application virtual machine via vagrant
  hosts: all
  become: true
  become_user: root
  remote_user: vagrant
  vars:
    update_apt_cache: true

  roles:
    - base
    - avahi
    - db
    - rabbitmq
    - web
    - celery
    - nginx
