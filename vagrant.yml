---

- name: Create application virtual machine via vagrant
  hosts: all
  become: yes
  become_user: root
  remote_user: vagrant
  vars:
    update_apt_cache: yes

  roles:
    - base
    - avahi
    - db
    - rabbitmq
    - web
    - celery
    - nginx
